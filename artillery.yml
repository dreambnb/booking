config:
  target: "http://localhost:7777"
  phases:
    - duration: 20
      arrivalCount: 5
scenarios:
  - name: "Browsing"
    flow:
    - loop:
      - get:
          url: "/{{ $randomNumber(1,10000000) }}"
          count: 5

