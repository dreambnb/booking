config:
  target: "http://localhost:7777"
  phases:
    - duration: 20
      arrivalCount: 20
scenarios:
  - name: "Browsing"
    flow:
    - loop:
      - get:
          url: "/booking/{{ $loopCount }}"
          count: 5

